#ifndef COMPLEX_HH
#define COMPLEX_HH

// Template Struct to chain Object and its Graphic shape
template <typename T, typename U>
struct Complex {
    T object;
    U objectShape;
};

#endif // COMPLEX_HH
